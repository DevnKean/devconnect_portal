{% extends ':emails:layout.html.twig' %}
{% block content %}
    <p>Hi {{ user }},</p>
    <p>Please click the link below to reset your password:</p>
    <p><a href="{{ absolute_url(url('reset_password_token', {'token': token})) }}">{{ absolute_url(url('reset_password_token', {'token': token})) }}</a></p>
{% endblock %}